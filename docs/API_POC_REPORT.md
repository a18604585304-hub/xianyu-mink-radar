# API POC 报告

日期：2026-08-29  
阶段：**未完成真实调用**

## 结论

**FAIL（资格未通过，尚无法测覆盖率）**

不是接口文档不存在，而是当前淘宝开放平台账号 **无法入驻闲鱼联盟类目**，因此拿不到 `alibaba.idle.affiliate.material.query` 的调用资格。

在人工确认企业资质 / 换主体之前，**不进入 Phase 1**。

## 已核实

| 项 | 结果 |
|---|---|
| API 是否仍存在 | 是。文档 apiId=71090，闲客联盟 |
| 是否免费 | 文档标记免费 |
| 是否需要授权 | 是，必须 session |
| keyword / in1day / create_time | 文档示例均存在 |
| 本账号创建闲鱼联盟应用 | **否**。企业资质认证未通过：支付宝不是企业资质 |
| 闲鱼电商类目 | 可点确认，但权限包是卖家商品管理，不含物料查询 |
| 真实 JSON | 无。无 AppKey/Session，未向 eco.taobao.com 发业务请求 |
| 覆盖率 / 发现延迟 | 无法计算 |

## Bark

第一条 Key 实测 `code=200`。第二条偶发 SSL 抖动后重试成功。

## 代码

Phase 0 脚手架已写：`scripts/test_xianyu_api.py`、`/health`、`/auth/taobao`、`/auth/callback`。无凭据时脚本退出码 2，不伪造商品。

## 下一步（必须你做）

现在需要你做：**给淘宝开放平台绑定企业支付宝资质**（或换一个已企业认证的主体账号）。

原因：闲鱼联盟（物料查询 API 所在类目）要求企业资质，我无法代替你完成认证/主体变更。

完成后告诉我一声，我继续：创建应用 → 拿 AppKey → OAuth → 真正跑 `抽刀貂` POC。
